CREATE DATABASE ECommerce;

CREATE TABLE customers(
  id INT NOT NULL AUTO_INCREMENT,
  name VARCHAR(255),
  email VARCHAR(255),
  address VARCHAR(255),
  
  PRIMARY KEY (id),
  UNIQUE (email)
);

INSERT INTO customers (name, email, address)
VALUES ('John', 'john@gmail.com', 'salem'),
('Doe', 'doe@gmail.com', 'chennai'),
('Max', 'max@gmail.com', 'namakkal'),
('Ross', 'ross@gmail.com', 'coimbatore'),
('Starc', 'starc@gmail.com', 'tiruppur');

CREATE TABLE orders(
  id INT NOT NULL AUTO_INCREMENT,
  customer_id INT NOT NULL,
  order_date DATE NOT NULL,
  total_amount DECIMAL(10, 2) NOT NULL,
  
  PRIMARY KEY (id),
  FOREIGN KEY (customer_id) REFERENCES customers(id)
);

INSERT INTO orders (customer_id, order_date, total_amount)
VALUES (1,'2026-01-01', 20),
(2, '2026-01-01', 30),
(1, '2026-02-10', 40),
(2, '2026-02-02', 100),
(3, '2026-02-03', 15),
(3, '2026-02-05', 13.85),
(4, '2026-02-05', 19.15),
(4, '2026-02-15', 20.25),
(5, '2026-02-06', 14.75),
(5, '2026-02-17', 10.00),
(5, '2026-02-18', 10.135);

UPDATE orders 
SET total_amount = 160.00
WHERE id = 5;

CREATE TABLE products(
  id INT NOT NULL AUTO_INCREMENT,
  name VARCHAR(255) NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  description VARCHAR(255),
  
  PRIMARY KEY (id)
);

INSERT INTO products (name, price, description)
VALUES ('iPhone15', 45000, 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.'),
('Lotus Biscoff classic', 260.80, 'Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.'),
('Mac mini M4', 59000, 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.'),
('Macbook air M4', 91000, 'Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.'),
('Lotus biscoff Vanilla', 275.60, 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.');

-- 4
-- Add a new column to the products table.
ALTER TABLE products
ADD discount DECIMAL(2, 2);

------------------------------------------------------------------------------------------------------------------------------------------------------
Query SQL:

SELECT * FROM customers; 
SELECT * FROM orders;
SELECT * FROM products;

-- 1
-- Retrieve all customers who placed orders in the last 30 days.
SELECT customers.name, 
order_date, total_amount FROM orders
RIGHT JOIN customers 
ON orders.customer_id = customers.id 
WHERE orders.order_date >= '2026-01-19';

-- 2
-- Get the total amount of all orders placed by each customer.
SELECT customers.name, customers.email, customers.address
SUM(total_amount) FROM orders
LEFT JOIN customers 
ON customers.id = orders.customer_id
GROUP BY customer_id;

-- 3
UPDATE products 
SET price = 45.00
WHERE id = 5;

-- 3
SELECT * FROM products WHERE id = 5;

-- 5 
-- Retrieve the top 3 products with the highest price.
SELECT * FROM products 
ORDER BY price DESC
LIMIT 3;

-- 6
-- Get the names of customers who have ordered PRODUCT A.

-- 7
-- Join the orders and customers tables to retrieve the 
-- customer's name and order date for each other.
SELECT customers.name, order_date, total_amount
FROM orders
RIGHT JOIN customers 
ON orders.customer_id = customers.id;

-- 8 
-- Retrieve the orders with a total amount greater than 150.00
SELECT customers.name, order_date, total_amount
FROM orders
RIGHT JOIN customers 
ON orders.customer_id = customers.id
WHERE orders.total_amount >= 150.00;

-- 9
-- Normalize the database by creating a seperate table for order items
-- and updating the orders table to reference the order_items table.

-- 10
-- Retrieve the avg. total of all orders.
SELECT AVG(total_amount)
FROM orders;